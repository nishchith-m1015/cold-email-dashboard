# sync-handoff

Write your command content here.

This command will be available in chat with /sync-handoff

Update the context file with the last changes for pickup in VS Code.

Update docs/PROJECT_CONTEXT.md with a detailed summary of all API, schema, and layout changes completed since the last sync. Conclude with a reminder to run 'git push'.